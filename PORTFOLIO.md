# SubFlow — 개인 구독 관리 플랫폼

> 흩어진 구독료를 한곳에 모아 **"내가 매달 얼마를 내고 있는지"** 를 한 화면에서 답해주는 서비스.
> 기획 · 설계 · 개발 · 배포 · 출시 준비까지 전 과정을 혼자 진행한 풀스택 프로젝트입니다.

| 항목 | 내용 |
|------|------|
| 기간 | 2026.03 ~ 진행 중 (커밋 75개) |
| 역할 | 1인 개발 (기획 · 백엔드 · 웹 · 모바일 · 인프라 · 브랜딩) |
| 구성 | FastAPI 백엔드 · React 웹앱 · React Native(Expo) 모바일 앱 · 정적 랜딩 |
| 배포 | 백엔드 Railway(`api.mysubflow.app`) / 랜딩 Cloudflare Pages(`mysubflow.app`) / 모바일 EAS 빌드 |
| 규모 | 백엔드 6.5K줄 · 웹 6.2K줄 · 모바일 7.2K줄 · 랜딩 1.9K줄 (약 21,800줄) |
| 데이터 | 테이블 11개, 서비스 카탈로그 30종+, 백엔드 테스트 41개 |

---

## 1. 왜 만들었나

구독 서비스는 개별 금액이 작아서 개별로는 신경 쓰이지 않지만, 합치면 큰 고정비가 됩니다.
문제는 이게 **한곳에 모여 있지 않다**는 점입니다.

- 서비스마다 결제 주기가 다르다 (월/연/분기/주)
- 통화가 섞여 있다 (Netflix는 원화, ChatGPT·Claude·Figma는 달러)
- 요금은 조용히 인상되고, 환율은 매일 움직인다
- 안 쓰는데 겹치는 구독(예: Netflix + Disney+ + Watcha)이 있어도 알아채기 어렵다

그래서 **"모든 구독을 월 단위 원화 하나의 기준으로 환산해 보여주고, 변화가 생기면 먼저 알려주는 것"** 을 제품의 핵심으로 잡았습니다.

### 차별화 포인트 4가지

| 기능 | 설명 |
|------|------|
| **환율 변동 감지** | 외화 구독의 원화 환산액이 움직이면 알림 |
| **중복 구독 감지** | 같은 카테고리에 겹치는 구독을 탐지해 경고 |
| **요금 인상 이력 추적** | 요금제별 가격 변동 이력을 저장하고 내 구독에 영향이 가면 알림 |
| **내 구독 뉴스 AI 요약** | 구독 중인 서비스 관련 뉴스를 수집하고 OpenAI로 요약 |

---

## 2. 아키텍처

```
                 ┌──────────────┐   ┌─────────────────────┐
   사용자 ──────▶│  Web (React) │   │ Mobile (Expo/RN)    │
                 └──────┬───────┘   └──────────┬──────────┘
                        │  Axios + JWT(Bearer) │
                        └───────────┬──────────┘
                                    ▼
                        ┌───────────────────────┐
                        │  FastAPI (async)      │
                        │  7 Routers            │
                        ├───────────────────────┤
                        │  Service Layer        │
                        │  auth / subscription  │
                        │  analytics / notify   │
                        │  news / renewal /     │
                        │  delivery / digest    │
                        ├───────────────────────┤
                        │  SQLAlchemy 2.0 async │
                        │  + Alembic            │
                        └───────────┬───────────┘
                                    ▼
                         PostgreSQL 16 (11 tables)

  [APScheduler]  뉴스 갱신 6h · 알림 배송 10m · 구독 자동갱신 매일 · 주간 다이제스트 매주
  [외부 연동]    Frankfurter(환율) · OpenAI(요약) · Resend SMTP(메일) · Expo Push(푸시)
```

### 기술 스택

**백엔드** — FastAPI · SQLAlchemy 2.0 (async) · Alembic · PostgreSQL 16 · Pydantic · JWT + bcrypt · APScheduler · httpx · aiosmtplib · slowapi

**웹** — React 19 + TypeScript · Vite · Zustand · React Router · Recharts · Tailwind CSS

**모바일** — Expo SDK · React Native · Expo Router (파일 기반 라우팅) · Zustand + AsyncStorage · Reanimated · Expo Push

**인프라** — Docker Compose · nginx (SPA 서빙 + `/api` 프록시로 same-origin 구성) · Railway · Cloudflare Pages/Registrar · EAS Build

---

## 3. 설계에서 고민한 지점

포트폴리오에서 가장 하고 싶은 이야기는 "무슨 기능을 만들었나"보다 **"어떤 문제를 어떻게 판단했나"** 입니다.

### 3-1. 금액 기준을 한 곳으로 모으기

결제 주기(주/월/분기/연)와 통화(KRW/USD/EUR/JPY)가 섞이면, 대시보드 총액과 개별 카드 금액이
서로 다른 기준으로 계산되기 쉽습니다. 그러면 "이 구독이 전체의 몇 %" 같은 비율 계산이 **조용히 틀립니다.**

→ `utils/cost.py`에 **"월 단위 KRW"** 단일 기준을 두고, 총액·비중·예산·분석이 전부 이 함수를 거치게 했습니다.
서비스마다 각자 환산하지 않도록 진입점을 하나로 좁힌 것이 핵심입니다.

```python
def to_monthly_cost(cost: Decimal, cycle: BillingCycle) -> Decimal:
    """결제 주기가 어떻든 한 달치로 환산한다."""
    match cycle:
        case BillingCycle.WEEKLY:    return cost * Decimal("4.33")
        case BillingCycle.MONTHLY:   return cost
        case BillingCycle.QUARTERLY: return cost / 3
        case BillingCycle.YEARLY:    return cost / 12
```

금액은 전 구간 `Decimal`로 다뤄 부동소수점 오차를 배제했습니다.

### 3-2. 환율 — "실시간"이라고 말하지 않기

무료 환율 API(Frankfurter)는 ECB 고시 기준이라 **영업일 1회** 갱신됩니다. 실시간 시세가 아닙니다.

→ 응답의 `date`를 그대로 보관해 UI가 "언제 기준 환율인지" 밝히도록 했고,
1시간 TTL 캐시로 호출량을 줄였으며, API 장애 시에는 하드코딩된 폴백 환율로 **서비스가 죽지 않게** 했습니다.
정확하지 않은 값을 정확한 척 보여주지 않는 것이 이 기능의 요구사항이라고 판단했습니다.

### 3-3. 토큰 테이블 없이 1회용 링크 만들기

비밀번호 재설정 / 이메일 인증에는 보통 토큰 테이블 + 만료 토큰 청소 배치가 따라옵니다.

→ 대신 **JWT 서명 키에 "바뀌면 안 되는 값"을 섞는 방식**을 썼습니다.
- 비밀번호 재설정 토큰 → 사용자의 **현재 비밀번호 해시**를 서명에 혼합
- 이메일 인증 토큰 → **대상 이메일 주소**를 서명에 혼합

비밀번호가 바뀌는 순간 기존 토큰의 서명이 스스로 깨집니다. **1회용 보장이 공짜로 따라오고,
만료 토큰 정리 작업도 필요 없습니다.** 테이블 하나를 늘리는 대신 서명의 성질을 이용한 선택입니다.

### 3-4. 사용자 열거(User Enumeration) 차단

- `/auth/forgot-password`는 **가입 여부와 무관하게 항상 200 + 동일 메시지**를 반환합니다.
  응답이 갈리는 순간 그것만으로 가입자 명단을 캐낼 수 있습니다. 프론트도 성공/실패를 구분해 보여주지 않습니다.
- 로그인 실패는 사유와 무관하게 **401로 통일**하고, 존재하지 않는 계정일 때도 더미 해시로
  bcrypt 검증을 수행해 **응답 시간을 균일화**했습니다 (타이밍 공격 완화).

### 3-5. 이메일 인증을 "필수"로 만들지 않은 이유

가입 시 인증을 강제하면, 메일이 스팸함에 빠지는 순간 **가입자를 통째로 잃습니다.**

→ **가입 후 인증(A안)** 을 택했습니다. 미인증 사용자도 앱은 그대로 쓰고, **알림 메일만 나가지 않습니다.**
이메일을 변경하면 인증 상태가 초기화되고 새 인증 메일이 발송됩니다.
그리고 기능 도입 마이그레이션에서는 **기존 사용자를 모두 인증됨으로 채웠습니다** —
소급 적용하면 이미 쓰고 있던 사람들의 알림이 조용히 끊기기 때문입니다.

### 3-6. i18n — 한국어 원문을 키로 쓰기

`t("dashboard.title")` 같은 키 방식은 키 작명 비용이 들고, 번역이 빠지면 **화면에 키 문자열이 그대로 노출**됩니다.

→ **한국어 원문 자체를 키로** 쓰고, 사전에 없으면 한국어를 그대로 반환하게 했습니다.
번역이 점진적으로 채워지는 동안에도 화면이 절대 깨지지 않습니다.

```tsx
const { t } = useTranslation();
<h1>{t("구독 관리")}</h1>
<p>{t("{n}건", { n: 3 })}</p>
```

동음이의어(예: 문맥에 따라 다르게 번역돼야 하는 단어)만 예외적으로 분리해 처리했습니다.

### 3-7. 알림 — 생성과 발송을 분리

알림 종류가 8가지입니다: 중복 구독 · 요금 변경 · AI 뉴스 · 요금 인상 뉴스 · 무료체험 만료 · 예산 초과 · 결제 임박 · 환율 변동.

→ **"파생 알림 계산 → DB 인박스 적재"** 와 **"미배송 건 실제 발송"** 을 분리했습니다.
인박스 조회 시점에 파생 알림을 동기화하고, 별도 스케줄러가 10분마다 미발송 건을 이메일(SMTP)·Expo 푸시로 내보냅니다.
발송 실패가 알림 데이터 자체를 잃게 만들지 않고, 재시도도 자연스럽게 처리됩니다.

### 3-8. 스케줄러 잡 실패 격리

APScheduler로 4개 잡을 돌립니다 (뉴스 6시간 / 배송 10분 / 구독 자동갱신 매일 / 주간 다이제스트 매주).
모든 잡은 개별 `try/except`로 감싸 **잡 하나의 실패가 앱을 죽이지 않도록** 했습니다.
외부 API(뉴스·환율·메일)에 의존하는 잡이 대부분이라, 외부 장애가 곧 서비스 장애가 되면 안 됩니다.

구독 자동 갱신은 지난 결제일을 주기만큼 전진시키면서 **말일 보정**(1/31 → 2/28)을 처리하고,
경과한 주기마다 결제 이력을 기록합니다.

---

## 4. 배포와 출시 준비

기능 구현에서 끝내지 않고 **실제로 사람이 쓸 수 있는 상태**까지 가져가는 것을 목표로 했습니다.

### 배포 구성

| 대상 | 호스팅 | 주소 |
|------|--------|------|
| 백엔드 + DB | Railway (관리형 Postgres) | `api.mysubflow.app` |
| 랜딩 + 약관 | Cloudflare Pages | `mysubflow.app` / `/privacy` / `/terms` |
| 모바일 | EAS Build (Android AAB/APK) | Play Console 등록 준비 |
| 웹앱 | (배포 예정) | `app.mysubflow.app` |

- 도메인은 Cloudflare Registrar에서 `mysubflow.app` 확보 (`subflow.app`/`.com`은 선점 상태)
- Render 무료 티어는 **콜드스타트 지연** 때문에 제외하고 Railway 유료 Hobby 선택
- 로컬은 Docker Compose 한 방(`db + backend + web`)으로 기동, nginx가 SPA를 서빙하고 `/api`를 백엔드로 프록시해 **same-origin** 구성

### 실제로 막혔던 문제들

**1) Railway가 던져주는 `DATABASE_URL`을 그대로 못 쓴다**
관리형 Postgres가 주는 URL은 `postgresql://...?sslmode=require` 형식인데, SQLAlchemy async는 `postgresql+asyncpg://`를 요구하고 `sslmode` 파라미터도 받지 않습니다.
→ `config.py`에서 드라이버를 자동 정규화하고 `sslmode`를 제거하도록 처리. Dockerfile CMD는 `$PORT`를 주입받게 수정.

**2) EAS Android 빌드가 3번 연속 실패**
원인을 하나씩 분리해서 잡았습니다.
- `react-native-worklets` 누락 — Reanimated 4의 필수 의존성인데 expo-router가 reanimated를 요구 → 설치 + babel plugin 등록
- `babel-preset-expo` 미설치 — `babel.config.js`가 참조하는데 직접 의존성에 없었음
- **로고 6개가 확장자만 `.png`이고 실제로는 JPEG** — Android AAPT의 `mergeReleaseResources`에서 실패.
  Pillow로 진짜 PNG로 재인코딩해 해결.

이 과정에서 EAS gradle 로그가 GraphQL → GCS 서명 URL → **brotli 압축**으로 내려온다는 걸 알아내야 로그를 읽을 수 있었습니다(gzip이 아님).

**3) Cloudflare Pages의 SPA 폴백이 인증 링크를 삼킨다**
`mysubflow.app/verify-email?token=...`이 HTTP 200을 주지만 내용은 랜딩 페이지였습니다.
Pages가 없는 경로를 `index.html`로 폴백하기 때문에, 사용자는 인증 대신 마케팅 페이지를 보게 됩니다.
→ 웹앱을 별도 서브도메인(`app.mysubflow.app`)에 분리 배포하고 백엔드 `APP_BASE_URL`을 그쪽으로 지정하는 방식으로 설계 확정.
**"200이 왔으니 정상"이 아니라 응답 내용을 확인해야 한다**는 걸 확인한 사례입니다.

### 보안 하드닝 체크리스트

- JWT (access + refresh) + bcrypt 해싱
- 비밀번호 강도 검증 (최소 8자, 영문 + 숫자)
- Rate limiting — 로그인 10회/분, 회원가입 5회/분 (slowapi)
- 사용자 열거 방지 (응답·타이밍 통일)
- `SECRET_KEY`·DB 비밀번호 환경변수 분리 + **기본값 사용 시 기동 경고**
- `X-Forwarded-For` 신뢰를 `TRUST_PROXY` 플래그로 제어 (기본 off — 프록시 없이 켜두면 IP 스푸핑으로 rate limit 우회 가능)
- CORS 허용 도메인 환경변수화

### 출시를 위해 준비한 것들

기능 코드 밖의 작업도 직접 했습니다.
- **법적 문서** — 이용약관 · 개인정보처리방침 (마크다운 + 웹 페이지 양쪽)
- **랜딩 페이지** — 실제 앱 UI 스크린샷/영상을 담은 기기 목업, 스크롤 서사, 한/영 토글, 모바일 전용 랜딩 별도 구성
- **브랜드 자산** — 로고/마크 3색 세트, iOS·Android 전 사이즈 앱 아이콘
- **스토어 자산** — 스토어 스크린샷 6종 + 피처 그래픽 (SVG로 생성 스크립트화)
- **마케팅** — 인스타그램 카드뉴스 템플릿
- **출시 체크리스트** — 도메인 → 웹 배포 → 스토어 심사까지 단계별 문서화 및 비용 산정

수익 모델은 **완전 무료 출시**로 결정했습니다. 인앱결제 수수료·정산 이슈를 피하고,
이 프로젝트의 목표를 매출이 아니라 **실사용자 확보와 실제 운영 경험**에 두었기 때문입니다.

---

## 5. 주요 기능

| 영역 | 기능 |
|------|------|
| 구독 관리 | 카탈로그 30종+에서 추가 / 커스텀 구독 직접 입력 / 상태(활성·일시정지·해지·체험) 관리 / CSV 내보내기 |
| 대시보드 | 월·연 총 지출, 활성 구독 수, 다음 결제일, 예산 현황 |
| 지출 분석 | 카테고리별 비중, 월별 추이(구독 시작일 기반 + 다음 달 예상), 3/6/12/24개월 기간 토글 |
| 캘린더 | 과거 결제 내역 + 미래 결제 예정을 한 달력에서, 서비스 로고 표시 |
| 타임라인 | 구독 변경 이력(생성·플랜 변경·해지 등) 추적 |
| 알림 | 8종 파생 알림 + 인박스 + 이메일/푸시 실제 발송 + 주간 다이제스트 |
| 인사이트 | 중복 구독 감지, 절약 제안 및 적용 플로우, 요금 인상 알림, 환율 알림 |
| 뉴스 | 구독 서비스 관련 뉴스 수집(6시간 캐시) + OpenAI 요약 모달 |
| 가족/공유 | 분담 인원(member_count) 설정 → 1인당 몫으로 지출 재계산 |
| 기타 | 다크모드, 한/영 다국어, 첫 가입 온보딩 4단계 |

---

## 6. 데이터 모델

테이블 11개. 카탈로그(services / service_plans)와 사용자 구독(subscriptions)을 분리한 것이 핵심 설계입니다.

- `users` — 계정 (UUID PK, 이메일 인증 상태)
- `categories` / `services` / `service_plans` — 서비스 카탈로그 (Netflix, Spotify 등 30종+와 요금제)
- `plan_price_history` — 요금제 가격 변동 이력 (인상/인하 추적의 근거 데이터)
- `subscriptions` — 사용자의 실제 구독 (카탈로그 참조 또는 커스텀)
- `payment_history` — 결제 이력 (자동 갱신 시 기록)
- `subscription_history` — 구독 변경 이벤트 로그
- `notification_settings` / `notifications` — 알림 설정과 발송 레코드
- `news_cache` — 뉴스 캐시 (link 기준 중복 제거)

카탈로그를 별도로 두었기 때문에 **"내가 직접 입력하지 않아도 요금 인상을 감지"** 하는 기능이 성립합니다.
사용자가 입력한 금액만 저장했다면 인상 여부를 알 방법이 없습니다.

---

## 7. 테스트

백엔드 pytest 41개 — 인증, 구독 CRUD, 분석 집계, 카테고리, 알림, 자동 갱신을 커버합니다.
보안 하드닝(응답 통일·rate limit) 이후에는 **변경된 보안 동작에 맞춰 테스트를 갱신**했고,
rate limiter는 테스트 환경에서 비활성화해 테스트 간 상태 오염을 막았습니다.

---

## 8. 현재 상태와 다음 단계

**완료**
- 백엔드 프로덕션 배포 (Railway + 관리형 Postgres, 커스텀 도메인 + SSL)
- 랜딩·약관 배포 (Cloudflare Pages)
- Android 프리뷰 빌드 성공 (EAS)
- 메일 발송 파이프라인 검증 완료 (Resend, 도메인 인증 + DKIM/SPF 설정)

**진행 예정**
- 웹앱을 `app.mysubflow.app`에 배포 (인증·비밀번호 재설정 링크 동작에 필요)
- Google Play 내부 테스트 → 프로덕션 심사 제출
- 소셜 로그인 (Google/Kakao) — 개발자 앱 자격증명 확보 후
- 실사용자 피드백 수집 및 개선

---

## 9. 이 프로젝트에서 얻은 것

1. **도메인 규칙을 코드 한 곳으로 모으는 판단** — 통화·주기 환산을 진입점 하나로 좁힌 것이 이후 모든 집계 기능의 신뢰도를 결정했습니다.
2. **불확실한 데이터를 정직하게 다루기** — 환율의 갱신 주기와 폴백을 UI 레벨까지 설계에 반영했습니다.
3. **보안을 기능이 아니라 기본값으로** — 사용자 열거 방지, 타이밍 균일화, 프록시 신뢰 기본 off 같은 "티 안 나는 결정"들.
4. **테이블을 늘리지 않고 요구사항을 만족시키기** — 서명 키에 상태를 섞어 1회용 토큰을 구현.
5. **배포는 코드가 끝나는 지점이 아니다** — 드라이버 URL 정규화, 빌드 툴체인 의존성, 확장자와 실제 포맷 불일치, CDN 폴백까지. 실제 배포에서만 만날 수 있는 문제들을 직접 해결했습니다.
6. **제품을 끝까지 밀어보기** — 법적 문서, 브랜딩, 스토어 자산, 비용 산정까지 포함해야 "출시"가 됩니다.
