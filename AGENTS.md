# Portfolio Project

윤경은의 개인 포트폴리오 웹사이트 (React + Vite)

## Build & Dev

- `npm run dev` — 개발 서버 (port 5175)
- `npm run build` — 프로덕션 빌드
- `npm run preview` — 빌드 결과 미리보기

## Tech Stack

- React 19 + React Router v7 (BrowserRouter)
- Vite 6 (빌드 도구)
- Tailwind CSS 4 + 컴포넌트별 CSS 파일 (하이브리드 스타일링)
- Framer Motion (애니메이션)
- Recharts (차트), React-Leaflet (지도)
- React Icons (아이콘)

## Project Structure

```
src/
├── components/   # 재사용 컴포넌트 (Navbar, Hero, Projects, Footer 등)
├── pages/        # 프로젝트 상세 페이지 (6개)
├── assets/       # 이미지, GIF, 썸네일
├── App.jsx       # 라우팅 설정
├── main.jsx      # 엔트리 포인트
└── index.css     # 글로벌 스타일 + Tailwind import
```

## Routing

- `/` — 메인 포트폴리오 (Hero, Projects, Research, Contact)
- `/projects` — 전체 프로젝트 갤러리
- `/projects/:slug` — 개별 프로젝트 상세 페이지 6개

## Conventions

- 컴포넌트/페이지 파일: PascalCase (.jsx)
- CSS 파일: lowercase, 컴포넌트와 같은 폴더에 배치
- 스타일링: Tailwind 유틸리티 우선, 복잡한 애니메이션은 CSS 파일 사용
- 애니메이션: Framer Motion fadeUp + stagger 패턴
- 레이아웃: max-w-7xl 컨테이너, px-8 패딩
- 언어: 한국어 콘텐츠 (lang="ko")
- 폰트: Inter (본문) + Syne (포인트)

---

## Portfolio Content Guidelines (포트폴리오 콘텐츠 원칙)

> 포트폴리오는 "프로젝트의 상세설명서"가 아니라 **"나의 상세설명서"**
> 나라는 사람이 어떻게 일하는지, 어떤 경험을 쌓았는지 소개하는 문서

### 핵심 원칙: 양보다 밀도

프로젝트 10개 나열보다 **[문제-해결-결과]가 명확한 1개**가 낫다.
단순 활동 나열은 일기장일 뿐, **문제 해결의 과정**이 담겨야 한다.

### 포폴 체크리스트 10

모든 프로젝트 상세 페이지는 아래 10가지를 만족하는지 점검:

1. **해결하려는 진짜 문제**가 한 문장으로 명확한가?
2. 문제를 증명할 **데이터나 리서치 결과**가 있는가?
3. 설정한 **가설이 논리적**으로 타당한가?
4. 수많은 대안 중 **왜 이 해결책을 선택**했는가?
5. 제품소개가 아니라 **나의 사고 과정**이 보이는가?
6. **기술적/현실적 제약을 어떻게 극복**했는가?
7. 프로젝트 내에서 **본인의 역할이나 기여도**가 보이는가? (팀 프로젝트)
8. 결과가 **지표나 숫자**로 명확히 증명되었는가?
9. 프로젝트 종료 후 얻은 **핵심 인사이트**는 무엇인가?
10. 이 포폴을 읽고 **같이 일하고 싶어지는가**?

### 프로젝트 상세 페이지 필수 섹션 구조

```
1. Hero — 프로젝트명, 한 줄 요약, 역할 배지, 기간
2. Problem — 해결하려는 문제 (한 문장) + 데이터/리서치 근거
3. Approach — 가설 설정 → 기술 선택 이유 (왜 이 해결책인가)
4. Architecture — 시스템 구조도
5. Key Features — 핵심 기능 (기능 나열 X, 문제→해결 맥락으로)
6. Technical Decisions — 기술적 의사결정과 트레이드오프
7. Results — 정량적 성과 (지표, Before/After, 숫자)
8. My Contributions — 본인 역할/기여도 (팀 프로젝트 필수)
9. Challenges — 문제 → 원인 → 해결 구조 (기술적 제약 극복)
10. Retrospective — 핵심 인사이트 + 아쉬운 점 + 다음에 하고 싶은 것
```

### 콘텐츠 작성 원칙

- **제품소개 금지**: "~기능이 있습니다" 나열 X → "~문제가 있어서 ~방식으로 해결했습니다" O
- **사고 과정 드러내기**: 왜 그 기술을 썼는지, 어떤 대안을 고려했는지, 어떤 트레이드오프가 있었는지
- **Before/After 구조**: AS-IS(기존 문제) → TO-BE(해결 후 상태)를 수치로 비교
- **직무 역량 키워드 연결**: 경험을 #문제해결 #데이터분석 #시스템설계 등 역량 키워드와 연결
- **팀 프로젝트는 기여도 명시**: "팀에서 무엇을 했다"가 아니라 "내가 이 부분을 맡아서 이렇게 해결했다"

### 경험을 "과정의 숫자"로 쪼개는 규칙 (최우선 적용)

> 숫자가 필요한 이유는 단 하나: 읽는 사람이 경험의 과정을 **직관적으로 이해**하게 돕는 가이드.
> 결과 숫자뿐 아니라 **과정의 숫자**(빈도, 기간, 횟수, 건수, 팀원수)를 반드시 포함해야 한다.

**BAD (모호표현 — 인사담당자가 "그래서 얼마나?"라고 묻게 되는 문장):**
- "다양한 레퍼런스를 분석했습니다"
- "팀원들과 협력했습니다"
- "꾸준히 개선했습니다"
- "경험이 있습니다" (과정 숫자 없이)

**GOOD (선명표현 — 과정을 숫자로 쪼갠 문장):**
- "매일 20분, 3개 이상 레퍼런스 분석 후 기록, 60주간 주 2회 트렌드 리포트 [누적 120건+]"
- "팀원 5명의 제작 일정을 조율해 기한 내 시리즈 8편 완결"
- "90일간 누락 없이, 주 3회 콘텐츠 발행 [총 40건+]"
- "3개 프로젝트에서 4중 Guardrail 설계 → 정확도 37%→85%로 개선한 경험"

**경험 쪼개기 카테고리:**
1. **협업**: 팀원 수, 조율한 일정, 기한, 산출물 개수
2. **분석력**: 분석 빈도, 분석 대상 개수, 기록 방식
3. **지속성**: 기간(일/주/월), 빈도(주 N회), 누적 건수
4. **기술 깊이**: 비교한 대안 수, 실험 횟수, 반복 이터레이션 수

**적용 대상:**
1. Core Competency / Summary — "경험이 있습니다" 문장에 과정 숫자 필수
2. CTA / Contribution 섹션 — "경험을 바탕으로" 문장에 구체적 프로젝트 수와 과정 숫자
3. 이력서 요약문 — 모든 "경험" 언급에 과정의 숫자 동반

### "했다"를 "해결했다"로 바꾸는 규칙 (최우선 적용)

> 모든 텍스트에서 "~개발", "~구현", "~구축", "~설계", "~적용" 문장을 찾아
> **[상황/문제] → [내가 한 행동] → [결과, 숫자로]** 구조로 변환해야 한다.

**BAD (했다 톤):**
- "Django REST Framework 기반 퀴즈 API 설계 및 CRUD 구현"
- "RAG 파이프라인 구축"
- "Nginx 리버스 프록시 구성"

**GOOD (해결했다 톤):**
- "단일 퀴즈 형식만 지원하여 학습 다양성 부족 → DRF 기반 3종 퀴즈 API 설계 → 퀴즈 유형 1→3개 확장"
- "초기 벡터 검색만으로 규정 문서 커버 불가 → HyDE+BM25+RRF+Reranker 파이프라인 → 정밀도 확보"
- "로컬 코드가 AWS에서 502 에러 → Nginx+Gunicorn 설정 디버깅 → 안정 배포 완료"

**적용 대상 (우선순위):**
1. CONTRIBUTIONS 배열 desc — 가장 중요. 반드시 [문제→행동→결과] 구조
2. Hero 설명문 — 한 문장 요약도 [문제→해결→결과 수치] 포함
3. CHALLENGES 배열 — 이미 Problem/Solution 구조이므로 result 숫자만 보강
4. 기능 설명 — collapsed 영역이라 우선순위 낮음

### 페이지 구조 — 보이는 것 vs 숨기는 것

**항상 보이게 (visible):**
- Hero, Problem/Motivation, Technical Decisions (문제→판단 과정)
- Evaluation & Verification (정량 검증 결과)
- My Contributions (상황→행동→결과 구조)
- Technical Challenges (문제→해결)
- Demo(시연) 또는 Screenshots(스크린샷) — Demo가 없으면 Screenshots
- Retrospective (핵심 인사이트)

**접어서 숨기기 (CollapsibleSection):**
- Overview Stats (숫자만 나열)
- Architecture (시스템 구조도)
- Tech Stack (기술 뱃지)
- Key Features (기능 나열)
- API Endpoints, DB Schema, Data Sources 등 기술 스펙
- Phase 비교표, 비교 테이블 등 부가 정보

### 프로젝트 유형별 주의사항

**개인 프로젝트 (CryptoVol Dashboard, Seoul Culture Map)**:
- 팀 분석을 개인이 서비스로 확장한 프로젝트
- "왜 서비스화가 필요했는가" 동기가 명확해야 함
- 분석 코드 → 서비스 코드 전환 과정의 기술적 의사결정 강조
- 팀에서 한 것 vs 개인이 확장한 것 명확히 구분

**팀 프로젝트 (PyMate, WorkFlow Agent, SubFlow 등)**:
- 본인 역할/담당 영역 반드시 명시
- 팀 전체 성과가 아닌 본인 기여 중심 서술
- 협업 과정에서의 의사결정 참여 강조

---

## Problem Solving 섹션 (메인 페이지 Experience)

> "해결해 본 문제의 규모와 상관없이, 개발과 관련한 문제 상황을 개선하거나 해결해 보신 사항을 최대한 구체적으로 작성해 주세요.
> 레거시 코드를 리팩토링 해보신 경험이 있다면 좋아요.
> 기술자로서 비즈니스의 문제를 해결했던 경험과 그 과정에서 활용하셨던 기술적 장점이 드러나는 사례를 자세히 적어주시면 좋아요."

### 작성 원칙

- **Problem → Solution → Result → Insight** 4단계 구조 필수
- 문제는 구체적 상황으로 (X: "성능이 안 좋았다" / O: "GARCH 적합이 수백ms 걸려 동시 요청 시 응답 지연")
- 해결은 "왜 그 방법을 선택했는가" 판단 근거 포함
- 결과는 반드시 정량적 지표 (Before → After 수치)
- Insight는 "이 경험에서 얻은 재사용 가능한 판단 기준"

### 현재 등록된 3개 사례

1. **분석 코드 → 실시간 API 전환** (CryptoVol Dashboard)
   - 캐싱 전략, WebSocket 릴레이, 에러 격리
   - 키워드: #캐싱전략 #WebSocket #실시간시스템 #에러격리

2. **Flask MVP → Django 프로덕션 전환** (PyMate)
   - 프레임워크 마이그레이션, RAG 임베딩 교체, 배포 안정화
   - 키워드: #프레임워크마이그레이션 #인프라설계 #RAG품질 #배포안정화

3. **LLM 과신 방지 서버 검증 시스템** (WorkFlow Agent)
   - 4중 Guardrail, 5-factor Confidence 보정, LoRA 데이터 품질 실험
   - 키워드: #LLM신뢰성 #Guardrail #데이터품질 #파인튜닝

### 새 사례 추가 시 기준

- 단순 기능 구현이 아닌 **문제 인식 → 판단 → 해결** 과정이 있는가
- 기술적 트레이드오프(성능 vs 정확도, 비용 vs 품질 등)가 드러나는가
- 결과가 지표로 증명 가능한가
- 서버 개발자 관점의 역량(API 설계, 성능 최적화, 인프라, 데이터 파이프라인, 검증 시스템)이 보이는가
